# This is a Databricks asset bundle definition for indra_dlt_pipeline_project1.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: indra_dlt_pipeline_project1
  uuid: cb47b849-72d5-4dbe-80dd-0c1450293503

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-0d473c91-2622.cloud.databricks.com
    variables:
      catalog: indrajit_mitra_dbx_catalog
      schema: dlt_demo
  prod:
    mode: production
    workspace:
      host: https://dbc-0d473c91-2622.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/indrajitmitra4@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/indrajitmitra4@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: indrajit_mitra_dbx_catalog
      schema: dlt_demo
    permissions:
      - user_name: indrajitmitra4@gmail.com
        level: CAN_MANAGE
